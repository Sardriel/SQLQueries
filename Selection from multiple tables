SELECT 

	  cards.Name
	  ,cards.token
	  ,orders.VariableSymbol
	  ,cards.UserProviderKey
      
FROM [MyDatabase].[dbo].[Table]
JOIN MyDatabase.dbo.Table on Cardid = cards.id
JOIN MyDatabase.dbo.Table on orderid = orders.id
WHERE orderid IN ()
